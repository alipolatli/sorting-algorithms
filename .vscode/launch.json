{
    "version": "0.2.0",
    "configurations": [
      {
        "name": ".NET Core Launch (debug)",
        "type": "dotnet",
        "request": "launch",
        "command": "${command:dotnet}",
        "args": ["run", "--debug"],
        "process": "${command:dotnet}",
        "console": "integratedTerminal",
        // Add this line:
        "project": "${workspaceFolder}/**.csproj"  // Adjust the pattern if needed
      }
    ]
  }
  